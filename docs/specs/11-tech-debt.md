# 11 — Технический долг и известные проблемы

Живой список. Каждый пункт: что наблюдали (с цифрами), гипотеза, что сделать, как проверить.

## Производительность и память

| # | Проблема | Наблюдение | Что сделать |
|---|---|---|---|
| P1 | Показ слоя из трея стартует через ~105 мс | Замер `IsWindowVisible`: 106–108 мс и в `36ffc38` (до анимаций), скрытие мгновенное | Найти, где eframe откладывает обработку события для скрытого корневого окна (`sleep_if_invisible_or_minimized` — 10 мс, не оно). Проверить `ControlFlow` при скрытом окне в glow_integration; вариант — показывать окно из `logic()` сразу, не дожидаясь кадра |
| P2 | Библиотека оставляет +19 МБ private после первого открытия | 104,8 → 118,4 → 123,5 МБ, дальше плато | Проверить, что остаётся: пулы драйвера NVIDIA после уничтожения GL-контекста, glyph-атлас egui (общий на все viewport), кеш `prepare_cached` в закрытом `Store`. Попробовать: общий GL-контекст для deferred viewport; сброс атласа шрифтов после закрытия |
| P3 | +8 МБ private при первом скрытии слоя | Разово, дальше стабильно | Та же гипотеза, что P2 |
| P4 | Полный промах в поиске — скан LIKE ~25 мс на 10 000 заметок | `search_speed` | Триграммный индекс FTS5 только для заметок > N, или отсечка LIKE по длине слова ≥ 4. Решать, когда у пользователя будет > 5 000 карточек |
| P5 | Первое открытие окна поиска/захвата вне бенча не мерится автоматически | Регресс до 190 мс нашли вручную (`DwmFlush`) | Добавить в `bench.rs` метрику «хоткей → `IsWindowVisible`», а не только «хоткей → первый кадр» |

## Корректность

| # | Проблема | Что сделать |
|---|---|---|
| C1 | Импорт делит размеры заметок с других мониторов на масштаб монитора слоя | Брать DPI исходного монитора (`GetDpiForMonitor`) — см. 06 |
| C2 | `card::free_slot` (захват, возврат из архива) не учитывает размеры карточек так, как `sticky::place` | Заменить на `place` с размером по умолчанию; одна функция размещения в `card.rs` |
| C3 | Win+D и перезапуск Explorer не проверены | Ручной сценарий в `scripts/verify-shell.ps1`: `taskkill /f /im explorer.exe` на тестовой машине/ВМ, проверка иконки трея и слоя |
| C4 | Хоткеи, занятые при старте, не перерегистрируются, когда освобождаются | Повторная попытка при открытии меню трея/настроек (см. 07) |
| C5 | `verify-shell.ps1` падает на проверке CPU, если в профиле активно предложение импорта | Скрипт должен запускаться с отдельным `LOCALAPPDATA` по умолчанию |
| C6 | Одновременное удаление карточки на слое и её анимация ухода при быстром «Отменить» < 160 мс дают две карточки с одним id в кадре | Снимать карточку из `leaving` при повторном появлении того же id |

## Архитектура

| # | Проблема | Что сделать |
|---|---|---|
| A1 | `app.rs` ~1150 строк: слой, карточки, тосты, настройки, бенч-хуки | Разделить: `layer.rs`, `card_ui.rs`, `toast.rs`; `app.rs` — оркестрация событий (до спек 04/06) |
| A2 | Три канала «окно → корень» (`bar::Outbox`, `library::Request`, `shell::Event`) | Один enum `Command` (см. палитру 05) и одна очередь |
| A3 | Схема мигрирует проверками `IF NOT EXISTS`/`pragma_table_info` | Таблица `schema_version` и нумерованные миграции в `store/migrations.rs`, тест «с каждой версии на последнюю» |
| A4 | Ничего не логируется при падении | Панический хук → `%LOCALAPPDATA%\Ebb\crash.log` (без текста заметок), `panic = "abort"` сохранить |

## Сборка и распространение

| # | Что | Заметки |
|---|---|---|
| D1 | Установщик | Portable exe + опционально MSIX. **Внимание**: в MSIX `%LOCALAPPDATA%` виртуализируется (замерено: +40 мс к старту и другая папка данных) — для MSIX хранить данные через `ApplicationData::LocalFolder` осознанно или не упаковывать |
| D2 | Автозапуск указывает на путь exe | После переноса exe — «Запускается другая копия» уже показывается в настройках; добавить кнопку «Исправить» |
| D3 | Подпись кода | Без подписи SmartScreen пугает при первом запуске |
| D4 | Обновления | `--quit` уже есть; обновлятель: скачать → `--quit` → заменить → запустить |

## Тесты

- UI-сценарии через egui_mcp оформить скриптами (сейчас — вручную в сессии): кнопки карточек,
  отмена, библиотека, импорт на синтетическом профиле.
- Бар и библиотеку egui_mcp не видит (только корневой viewport) — либо научить инспектор
  подключаться к deferred viewport, либо держать PowerShell-проверки (`scripts/`).
